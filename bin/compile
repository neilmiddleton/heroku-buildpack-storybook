#!/bin/sh

BUILD_DIR=$1

cd $BUILD_DIR

if [ -f "static.json" ]
then
  echo '       Overwriting existing `static.json`'
fi

echo '{ "root": ".out/", "clean_urls": true, "basic_auth": true   }' > static.json

rm Procfile
node --max_old_space_size=2560 yarn build-storybook
