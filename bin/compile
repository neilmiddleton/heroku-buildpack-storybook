#!/bin/sh

BUILD_DIR=$1

cd $BUILD_DIR

if [ -f "static.json" ]
then
  echo '       Overwriting existing `static.json`'
fi

echo '{ "root": ".out/", "clean_urls": true, "basic_auth": true   }' > static.json

rm Procfile
yarn install
yarn build-storybook
